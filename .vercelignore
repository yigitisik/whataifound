# Authoring + local-preview toolchain. Neither runs on Vercel: the site is static,
# and feed.xml/feed.json are generated locally and committed. Keeping these out of
# the deployment keeps the CDN bundle to exactly what is served.
serve.py
build-feed.py

# Repo docs, not site content.
README.md
SCHEMA.md
.claude/
